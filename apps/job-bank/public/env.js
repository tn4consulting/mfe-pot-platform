// Overwritten by the container entrypoint script at startup with real
// values from the Helm chart's ConfigMap -- see CLAUDE.md's Hosting
// section. Empty under plain `nx serve`, where dev-time defaults apply.
window.__mfePotEnv = {};
