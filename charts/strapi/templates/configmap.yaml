apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.strapi.name }}-env
  labels:
    app.kubernetes.io/name: {{ .Values.strapi.name }}
    app.kubernetes.io/component: cms
data:
  {{- range $key, $value := .Values.strapi.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
