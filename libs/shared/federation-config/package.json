{
  "name": "@tn4consulting/shared-federation-config",
  "version": "0.6.0",
  "description": "Shared Native Federation config (react.js's shared-singleton block) for every mfe-pot app. The Angular-shaped index.js (and its @angular-architects/native-federation peer dependency) was deleted in 0.6.0 -- react.js's pattern is now this package's only export, once every consumer converted to React.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tn4consulting/mfe-pot-platform.git"
  },
  "type": "module",
  "main": "./src/react.js",
  "exports": {
    "./react": "./src/react.js"
  },
  "peerDependencies": {
    "@softarc/native-federation": ">=4.0.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": [
    "src"
  ]
}
